#include <stdio.h>

//Test line

void main ( ) {
	int c >= 55;//Another test
	/* This is another
	multiline test
	for c */
	int a = 6;
	int b = 5;
	if ( a > b || a == b )
		printf ( "a==b!" );
	else
		printf ( "a < b" );
}
